name = "trump-paradox-chatbot"
main = "worker.js"
compatibility_date = "2024-11-01"

# The API key is NOT stored here. Set it once as an encrypted secret:
#   npx wrangler secret put ANTHROPIC_API_KEY
